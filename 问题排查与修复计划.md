# 功能异常排查报告与修复计划

**排查时间**: 2026-06-08  
**排查范围**: 拆书上传功能 + 写小说运行功能

---

## 一、问题概述

### 1. 拆书上传功能异常
- **现象**: 用户报告"拆书的上传功能用不了"
- **影响范围**: 文件上传（upload tab）和粘贴正文（paste tab）两种上传方式

### 2. 写小说功能未正常运行
- **现象**: 用户报告"写小说也没有正常的运行"
- **影响范围**: 项目启动后的创作流程执行

---

## 二、根因分析

### 问题 1: SQLite 数据库锁冲突（Database is Locked）

#### 核心问题
在高并发写入场景下，SQLite 数据库出现 `database is locked` 错误，导致：
1. **拆书上传失败**: POST `/api/study/materials/from-text` 返回 500 Internal Server Error
2. **文件批量上传失败**: POST `/api/study/materials/upload/batch` 返回数据库锁错误
3. **项目启动失败**: POST `/api/projects/{id}/launch` 返回 500 Internal Server Error

#### 测试验证结果
```bash
# 测试1: 粘贴正文上传
curl -X POST 'http://127.0.0.1:8000/api/study/materials/from-text' \
  -H 'Content-Type: application/json' \
  -d '{"title":"测试书籍","raw_text":"这是测试内容。",...}'
结果: Internal Server Error (500)

# 测试2: 文件批量上传  
curl -X POST 'http://127.0.0.1:8000/api/study/materials/upload/batch' \
  -F 'files=@test_upload.txt'
结果: {"ok":true,"data":[{"ok":false,"filename":"test_upload.txt",
       "error":"OperationalError: (sqlite3.OperationalError) database is locked"}]}

# 测试3: 项目启动
curl -X POST 'http://127.0.0.1:8000/api/projects/1/launch' \
  -d '{"mode":"semi_auto","outline_text":"第1章|测试章节|这是测试"}'
结果: Internal Server Error (500)
```

#### 根本原因
1. **长时间事务阻塞**: 
   - DeepStudy Worker 后台运行，正在处理 material_id=1 的拆书任务（1167章节）
   - Worker 持有写锁长达数秒，导致其他写入请求被阻塞
   
2. **SQLite 配置已优化但仍不足**:
   - `database.py` 已配置 WAL 模式 + busy_timeout=5000ms
   - 但当 Worker 处理大量章节时，5秒 timeout 仍然不够
   
3. **并发写入冲突点**:
   - API 请求（FastAPI uvicorn 进程）
   - Worker 后台任务（同一进程内的 asyncio 任务）
   - 两者同时写入时发生锁竞争

#### 当前数据库状态
```json
{
  "ok": true,
  "data": [{
    "id": 1,
    "title": "剑来请自重",
    "chapter_count": 1167,
    "study_status": "studying",
    "study_progress": {
      "run_id": 1,
      "total_chapters": 1167,
      "processed_chapters": 0,
      "current_stage": null,
      "errors": [],
      "auto_started": true
    }
  }]
}
```
material_id=1 有 1167 章，DeepStudy 正在后台处理，持续占用数据库写锁。

---

### 问题 2: Worker 运行但未处理任务

#### Worker 状态检查
```json
{
  "state": "running",
  "loop_state": "alive",
  "worker_id": "worker-c9af0f86cf",
  "current_task_id": null,
  "domain_statuses": {
    "writing": {"running": false, "tasks_processed": 0},
    "deepstudy": {"running": true, "tasks_processed": 0, "errors_count": 0},
    "discussion": {"running": false, "tasks_processed": 0},
    "review": {"running": false, "tasks_processed": 1},
    "memory": {"running": true, "errors_count": 6}
  }
}
```

#### 观察结果
1. **Worker 已启动**: `state: "running"`, `loop_state: "alive"`
2. **DeepStudy domain 正在运行**: `deepstudy.running: true`
3. **但无任务进展**: `tasks_processed: 0`, `processed_chapters: 0`
4. **Memory domain 有 6 次错误**: 可能影响整体处理流程

#### 推测原因
1. DeepStudy Worker 卡在数据库锁上，无法推进 run_id=1 的处理
2. Memory domain 的 6 次错误可能与数据库锁或 LLM 调用失败有关
3. 由于数据库锁冲突，Worker 内部的 commit/flush 操作被阻塞

---

## 三、修复方案

### 方案 A: 提高数据库锁超时时间（推荐）

#### 修改内容
**文件**: `backend/app/core/database.py`

```python
# 当前配置 (第 32 行)
connect_args={"check_same_thread": False, "timeout": 30.0}

# 当前 PRAGMA (第 59 行)
cursor.execute("PRAGMA busy_timeout = 5000")

# 修改为
connect_args={"check_same_thread": False, "timeout": 60.0}  # 30s → 60s
cursor.execute("PRAGMA busy_timeout = 30000")  # 5s → 30s
```

#### 优点
- 最小改动，风险低
- 适用于大型书籍（1000+ 章节）的长时间事务
- 不改变业务逻辑

#### 缺点
- 治标不治本，如果事务超过 30 秒仍会失败
- 用户请求响应时间可能变长（最多等待 30 秒）

---

### 方案 B: 分离 Worker 进程（根本性解决）

#### 修改内容
1. **创建独立 Worker 进程脚本**  
   **文件**: `backend/start_worker.py` (新建)
   ```python
   import asyncio
   from app.workers.worker import get_worker
   
   async def main():
       worker = get_worker()
       await worker.start()
       # 保持运行
       while True:
           await asyncio.sleep(1)
   
   if __name__ == "__main__":
       asyncio.run(main())
   ```

2. **修改启动脚本**  
   **文件**: `start.bat` 或 `start.sh`
   ```bash
   # 启动 FastAPI
   start uvicorn app.main:app --host 0.0.0.0 --port 8000
   
   # 启动 Worker（独立进程）
   start python backend/start_worker.py
   
   # 启动前端
   npm run dev
   ```

3. **配置独立的数据库连接池**  
   **文件**: `backend/app/workers/worker.py`
   ```python
   # 为 Worker 创建独立的 engine 和 session_maker
   from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker
   
   worker_engine = create_async_engine(
       settings.database_url,
       connect_args={"check_same_thread": False, "timeout": 120.0},  # 更长的超时
       pool_size=5,  # 独立连接池
   )
   worker_session_maker = async_sessionmaker(worker_engine, ...)
   ```

#### 优点
- **根本性解决**: 完全隔离 API 和 Worker 的数据库连接
- **更好的资源管理**: Worker 可以配置更大的超时和连接池
- **更好的可观测性**: 可以单独监控和重启 Worker
- **符合生产最佳实践**: 后台任务应该独立进程

#### 缺点
- **改动较大**: 需要修改启动流程
- **部署复杂度增加**: 需要管理两个进程
- **开发环境需要调整**: 需要同时启动两个进程

---

### 方案 C: 优化事务粒度（配合方案 A）

#### 修改内容
**文件**: `backend/app/services/deepstudy/stages/base.py`

在每个 stage 处理完一批章节后立即 commit，而不是等待整个 run 完成：

```python
async def process_chapters(self, run: StudyRun, chapters: list, db: AsyncSession):
    for i, chapter in enumerate(chapters):
        await self._process_single_chapter(chapter, db)
        
        # 每处理 10 个章节就 commit 一次（释放锁）
        if (i + 1) % 10 == 0:
            await db.commit()
            logger.info(f"Batch commit: {i+1}/{len(chapters)} chapters")
    
    # 最终 commit
    await db.commit()
```

#### 优点
- 缩短单个事务持有锁的时间
- 提高并发写入成功率
- 即使 Worker 崩溃，已处理的章节也不会丢失

#### 缺点
- 需要仔细审查代码，避免破坏事务一致性
- 可能需要调整多个 stage 的代码

---

## 四、推荐修复步骤（分阶段执行）

### 阶段 1: 快速修复（立即执行）

**目标**: 让用户能够正常使用上传和启动功能

1. **应用方案 A**: 提高数据库锁超时时间
   ```python
   # backend/app/core/database.py
   connect_args={"timeout": 60.0}
   cursor.execute("PRAGMA busy_timeout = 30000")
   ```

2. **暂停 DeepStudy Worker**（临时措施）
   ```bash
   curl -X POST http://127.0.0.1:8000/api/worker/pause
   ```

3. **验证修复**
   - 测试拆书上传（文件 + 粘贴正文）
   - 测试项目启动（半自动 + 全自动）
   - 确认不再出现 500 错误

4. **重启服务**
   ```bash
   # 停止当前服务
   # 重新启动 backend 和 frontend
   ```

**预计时间**: 15 分钟  
**风险等级**: 低  
**回滚方案**: 恢复原配置值并重启

---

### 阶段 2: 优化事务（1-2 天内）

**目标**: 减少长时间锁占用，提高并发性能

1. **实施方案 C**: 在 DeepStudy stages 中增加批量 commit
2. **测试 Worker 处理大型书籍（1000+ 章节）**
3. **监控数据库锁等待时间和失败率**

**预计时间**: 2-4 小时开发 + 1 天测试  
**风险等级**: 中  
**回滚方案**: Git revert 相关 commits

---

### 阶段 3: 架构升级（长期规划）

**目标**: 生产级的任务处理架构

1. **实施方案 B**: 分离 Worker 进程
2. **引入任务队列** (可选):
   - 使用 Celery + Redis
   - 或使用 ARQ (异步 Redis Queue)
3. **升级到 PostgreSQL** (可选):
   - SQLite 不适合高并发写入
   - PostgreSQL 有更好的锁机制和并发性能

**预计时间**: 1-2 周  
**风险等级**: 高  
**回滚方案**: 保留原架构作为备份

---

## 五、验证清单

修复完成后，需要验证以下功能：

### 拆书上传
- [ ] 文件上传（单个 txt/md 文件）
- [ ] 批量上传（5 个文件）
- [ ] 粘贴正文上传
- [ ] 上传后自动分章
- [ ] 上传后自动创建 DeepStudy run

### 写小说启动
- [ ] 半自动模式（提供大纲）
- [ ] 半自动模式（提供人物）
- [ ] 半自动模式（提供设定）
- [ ] 全自动模式
- [ ] 启动后 Worker 正常处理任务

### Worker 运行
- [ ] Worker 状态为 running
- [ ] DeepStudy domain 正常处理章节
- [ ] Memory domain 无重复错误
- [ ] 章节处理进度正常推进

### 数据库性能
- [ ] 无 "database is locked" 错误
- [ ] API 响应时间 < 5 秒
- [ ] Worker 可同时处理多个 run

---

## 六、后续优化建议

1. **添加任务重试机制**
   - 数据库锁冲突时自动重试 3 次
   - 指数退避策略（1s, 2s, 4s）

2. **添加监控和告警**
   - 数据库锁等待时间指标
   - Worker 处理失败率告警
   - API 500 错误告警

3. **优化 DeepStudy 流程**
   - 支持断点续传
   - 支持并行处理多个章节
   - 支持暂停/恢复功能

4. **文档更新**
   - 更新部署文档，说明 Worker 启动流程
   - 更新故障排查文档，记录常见问题

---

## 七、总结

### 问题根源
两个功能的异常都源于 **SQLite 数据库锁冲突**：
- DeepStudy Worker 长时间占用写锁处理 1167 章节
- API 请求（上传、启动）无法获取写锁而超时失败

### 修复优先级
1. **P0 (立即)**: 提高数据库超时时间 + 暂停 Worker（方案 A）
2. **P1 (本周)**: 优化事务粒度（方案 C）
3. **P2 (下周)**: 分离 Worker 进程（方案 B）

### 预期效果
- 阶段 1 修复后：用户可正常上传和启动，但仍有性能瓶颈
- 阶段 2 修复后：大幅减少锁冲突，支持并发操作
- 阶段 3 修复后：生产级稳定性，支持高并发场景

---

**报告人**: AI 助手  
**审核建议**: 建议先执行阶段 1 快速修复，验证通过后再规划后续优化
