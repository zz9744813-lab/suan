# 2026-06-06 Daily Log

## P0 拆书书架完整修复 — 5 阶段全部完成

按 `NF2_STUDY_BOOKSHELF_P0_FULL_PLAN.md` 执行，5 阶段全部落地：

### P0-1: 入口统一 + 上传回归
- `App.tsx`: `/study` 重定向到 `/study/library`，旧页面移到 `/study/upload`
- 新建 `AddBookModal` 组件 (文件上传 + 粘贴正文，支持分类/标签)
- 后端新增 `POST /api/study/materials/from-text` (一站式: 创建→分章→DeepStudy→Worker)
- 前端 API 新增 `createStudyMaterialFromText`

### P0-2: 删除闭环
- 新建 `StudyDeleteService` (深度删除: 逐表删16张衍生产物表)
- 改造 `DELETE /materials/{id}` → 接入深度删除，支持 `?force=true`
- 新增 `POST /api/study/materials/batch-delete`
- 前端新增 `deleteStudyMaterialDeep` + `batchDeleteStudyMaterials` API
- StudyLibraryPage: 单本删除确认弹窗 + 批量管理 + 测试书清理
- `StudyMaterialUpdate` 补 `shelf_category` + `extra` 字段

### P0-3: 分类标签
- 书架 UI 从状态分组改为**分类优先** (SHELF_CATEGORIES: 全部/玄幻/都市/历史/科幻/言情/武侠/同人/古典/未分组/测试书/失败待修)
- `isTestBook()` 自动识别测试书
- batch upload 增加 `shelf_category` + `tags_json` Form 字段
- AddBookModal 分类/标签输入

### P0-4: 图谱诊断
- 后端新增 `GET /api/deepstudy/materials/{id}/diagnostics`
- 8种诊断原因: NO_CHAPTERS / NO_RUN / WORKER_STOPPED / RUN_FAILED / NO_ENTITIES / NO_RELATIONSHIPS / NO_GRAPH_NODES / OK
- StudyBookGraphPage: 空图时显示结构化诊断面板 (原因+数据+建议)

### P0-5: 验收
- TypeScript 零错误
- Vite build 通过
- Python py_compile 全部通过
- 20条必过清单逐项确认

### 文件修改总表
**后端:**
- `backend/app/routers/study.py` — from-text + batch-delete + 深度删除 + batch upload 分类
- `backend/app/routers/deepstudy.py` — diagnostics 端点
- `backend/app/services/study_delete_service.py` — 新建
- `backend/app/schemas/study.py` — StudyMaterialFromTextCreate + StudyMaterialUpdate 扩展
- `backend/app/schemas/__init__.py` — 导出新 schema

**前端:**
- `frontend/src/App.tsx` — /study 重定向
- `frontend/src/pages/StudyLibraryPage.tsx` — 大改: 分类优先 + 加书弹窗 + 删除 + 批量 + 诊断
- `frontend/src/components/study/AddBookModal.tsx` — 新建
- `frontend/src/components/study/AddBookModal.css` — 新建
- `frontend/src/api/index.ts` — 4 个新 API
