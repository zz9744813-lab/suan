# 2026-06-04 P7 执行记录

## P7：Prompt 类型矩阵 — 拖拽选型 + 可追溯 + 手动创建

### 完成的工作

#### P7.0 数据库层
- 新建 `backend/app/models/genre_prompt_map.py`：2 张表
  - `GenrePromptMapping`：Agent × Genre → PromptTemplate 绑定（unique constraint on agent_role_key+genre+prompt_template_id）
  - `ProjectPromptSnapshot`：章节 pipeline 运行时的提示词快照（traceability）
- `PromptTemplate` 新增 `immutable: bool = False` 字段（seed 模板不可删）
- `backend/app/models/__init__.py` 更新导入

#### P7.1 后端 API
- 新建 `backend/app/schemas/genre_prompt.py`：8 个 schema（GenrePromptMappingRead, MatrixCell, GenrePromptMatrixResponse, PromptSnapshotDetail, PromptTemplateCreate, TemplateUsageRead 等）
- 新建 `backend/app/routers/genre_prompts.py`：8 个端点
  - GET /matrix, PUT /bind, DELETE /unbind, PUT /reorder, GET /available
  - GET /projects/:pid/prompt-audit, GET /projects/:pid/chapters/:cid/prompt-audit
  - GET /templates/:id/usage
- `backend/app/routers/prompts.py` 追加 3 个端点：POST /templates（创建）, DELETE /templates/:id（删除，immutable 拒绝）, GET /templates/:id/usage（已移到 genre_prompts）
- `backend/app/schemas/__init__.py` 更新导入
- `backend/app/main.py` 注册 genre_prompts 路由

#### P7.2 引擎层
- `backend/app/services/prompt_engine.py` 新增 `resolve_for_agent()` 方法：
  - 查 genre_prompt_mapping（精确 genre → fallback genre="" → 回退硬编码 prompt_key）
  - RenderedPrompt 新增 genre 字段
- `backend/app/agents/base.py`：
  - AgentContext 新增 `project_genre: str | None`
  - `run()` 方法优先调用 `resolve_for_agent()`，None 时 fallback 旧逻辑
- `backend/app/workers/pipeline.py`：
  - 加载 project.genre 注入到所有 AgentContext
  - pipeline 完成后调用 `_write_snapshot()` 写入 ProjectPromptSnapshot

#### P7.3 种子数据
- `backend/app/prompts/default/library.py` 新增 `GENRE_PROMPTS`：5 个类型专属 Drafter 模板
  - drafter_urban_smooth（都市爽文流）
  - drafter_scifi_hard（科幻硬核风）
  - drafter_historical（历史厚重风）
  - drafter_suspense（悬疑节奏流）
  - drafter_romance（言情细腻风）
- `backend/app/seed.py`：
  - 导入 GENRE_PROMPTS + GenrePromptMapping
  - seed 模板标记 immutable=True
  - 自动 seed 5 个 genre 模板 + v1 版本
  - 写入 11 条默认 GenrePromptMapping（drafter × 5 genres + 5 agents generic fallback）

#### P7.4-7.6 前端
- 新建 `frontend/src/pages/GenrePromptMatrixPage.tsx` + `.css`
  - 矩阵表格：Agent × Genre
  - 模板池（可拖拽，使用 @dnd-kit）
  - 双击空白格打开模板选择器
  - 右键菜单解绑
  - 追溯面板：选项目 → 查看各章节快照
  - 新建模板表单
- `frontend/src/App.tsx`：注册 /prompts-matrix 路由
- `frontend/src/components/layout/RailNav.tsx`：新增「Prompt矩阵」导航入口
- `frontend/src/pages/PromptsPage.tsx`：+ 新建模板按钮 + 矩阵链接
- `frontend/src/api/index.ts`：新增 11 个 P7 API 函数
- `frontend/src/types/index.ts`：新增 P7 类型 + PromptTemplate.immutable

### 验证
- TypeScript 编译通过（零错误）
- 后端模型导入和路由注册验证通过
