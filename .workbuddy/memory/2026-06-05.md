# 2026-06-05

## UI 设计评审
- 完成 NovelForge 2.0 前端 UI 全面评审，输出详细报告至 `.workbuddy/artifacts/novelforge-ui-review.md`
- 评审覆盖 tokens.css、global.css、AppShell、RailNav、Shelf 组件库、Dashboard 等全部样式文件

## S1-S4 Bug修复 + 新功能实现（P0 Model Failover + NF2 自动工厂）

### S1 — 核心 Bug 修复（全部完成）
- **BUG-1**: `llm/client.py _prepare_payload()` — JSON 系统提示改为仅在 `response_format.type==json_object` 或 `require_json=True` 时注入，修复正文污染
- **BUG-2**: `model_selector.py L431` — fallback 候选评分从固定 `0.1` 改为真实评分（provider 健康分 × 0.75 衰减），上限 0.65
- **BUG-3**: `models/task.py` 新增 `not_before_at` 字段；`worker.py _tick()` 排除未到时间任务；`_mark_task_failed()` 改为指数退避重试（30s/60s/120s，最大 5min），耗尽 max_retries 才标 failed

### S2 — Fallback 链 + 健康探针统一（全部完成）
- **BUG-4+5**: `router.py _try_fallback()` 重写为多候选循环链（最多 MAX_FALLBACK_ATTEMPTS=2 次），跳过已失败 provider/model，每次失败更新熔断记录
- **BUG-6**: `provider_health.py` — 轻量探针不再覆盖 UI 探针的 `last_health_full`，只更新 `auto_probe` 子段，保留 `results/recommended_roles/score`

### S3 — AgentRunRecorder + Observability API（完成）
- 新建 `services/agent_run_recorder.py`: `get_summary()`, `get_recent_events()`, `get_provider_stats()`
- 新建 `routers/model_observability.py`: 4 个端点 `/observability/summary|events|providers|runtime-stats`
- 注册到 `main.py`

### S4 — PromptAutoBinder（完成）
- 新建 `services/prompt_auto_binder.py`: 按 agent_key 前缀匹配模板，结合历史 effect_score + genre 关键词评分，写入 `genre_prompt_mappings`（source="auto"）
- `prompt_engine.py resolve_for_agent()` Step 3 集成 AutoBinder，找不到映射时自动触发绑定

### 文件清单
修改: llm/client.py, model_selector.py, worker.py, models/task.py, llm/router.py, provider_health.py, prompt_engine.py, main.py
新建: services/agent_run_recorder.py, routers/model_observability.py, services/prompt_auto_binder.py
- 核心发现：
  - 优势：设计 Token 系统成熟、书架隐喻出色、4区网格布局优秀
  - P0 问题：全局字体偏小(14px base)、Unicode 图标不可靠、颜色对比度多项不达标、缺少 focus-visible 样式
  - P1 问题：颜色 Token 命名混乱(accent-gold 实际为蓝色)、布局大量 !important、Dark Mode 层次不够、缺少 loading skeleton 和 toast 系统
  - 优化方案分五个阶段，预计总工时 17-25 天

## 优化版演示页面
- 创建了 `novelforge-optimized.html`（单文件，~550行），展示所有优化建议的实际效果
- 核心改进：Fraunces+DM Sans 字体搭配、15px base 字号、SVG 内联图标（lucide-style）、4级暗色层次、skeleton loader、toast 通知、focus-visible 无障碍、tooltip 导航提示、渐变响应式降级（1300/1000/700px）
- 保留了原创 4 区网格布局和冷峻高效工作台的品牌调性

## 行为模式库角色卡化改造 (P0+P1)
- 按 `F:/NF2_behavior_mode_card_technical_plan.md` 技术方案实施
- **P0 后端完成**：6 张新表 + Model + Schema + Service + Router + 种子数据（6 分类 + 3 种子卡）
- **P1 前端完成**：三栏布局重写 BehaviorPage.tsx，新增 API 函数和 CSS
- API 已验证全部正常，TypeScript 编译零错误
- **待做**：P2 拖拽归类、P3 拆书自动提炼、P4 接入写作 Agent、P5 清洗评分

## 讨论室自动留痕重构 (P0-P5 全部完成)
- 按 `F:/NF2_discussion_room_auto_trace_technical_plan.md` 技术方案实施
- **后端**：
  - 新增 6 张表：discussion_threads, discussion_messages, discussion_issue_sources, discussion_skill_drafts, discussion_recycle_jobs, skills
  - 新增 Model: `app/models/discussion_trace.py`
  - 新增 Schema: `app/schemas/discussion_trace.py`
  - 新增 Router: `app/routers/discussion_trace.py`（10 个 API 端点）
  - 新增 Service: `app/services/discussion_trace.py`（IssueDetectorService + SkillBuilderService）
  - 新增 Agent: `app/agents/discussion_orchestrator.py`（DiscussionOrchestrator + Chief + SkillBuilder Agent）
  - 新增 Worker: `app/workers/discussion_worker.py` + `discussion_recycle_worker.py`
  - Worker 注册到主 worker.py 的 _run_forever 循环
  - rewrite_from_discussion 加入 SUPPORTED_TASKS
- **前端**：
  - 重写 DiscussionPage.tsx 为三栏布局（左侧讨论队列 / 中间时间线 / 右侧成果面板）
  - 替换原有手动创建讨论表单
  - 新增 12 个 API 函数
  - 新增 CSS：~300 行
  - TypeScript 编译零错误
- **核心流程**：Critic/Reader/Continuity → IssueDetector 自动创建线程 → Worker 自动跑 Agent 讨论 → Chief 收敛 → 创建 Rewrite 任务 + Skill 草案 → 7 天回收 + Skill 固化
- 旧 discussion_sessions/turns 表保留兼容

## Agent 分层记忆池 (P10 全部完成)
- 按 `F:/NF2_project_memory_layered_agent_technical_plan.md` 技术方案实施
- **后端**：
  - 新增 6 张表：agent_memory_entries, agent_memory_links, agent_memory_audit_logs, agent_memory_consolidation_jobs, agent_memory_access_logs, memory_change_requests
  - 新增 Model: `app/models/agent_memory.py`（含 tags/content_preview/source_payload property）
  - 新增 Schema: `app/schemas/agent_memory.py`（27 个 Pydantic schema）
  - 新增 Router: `app/routers/agent_memory.py`（15+ API 端点 + change_router 3 端点）
  - 新增 Service: `app/services/agent_memory_service.py`（6 个服务类）
  - Worker: memory_consolidation 后台任务（60s 间隔自动过期临时记忆）
  - Seed: 7 条种子记忆（2 permanent + 3 long_term + 1 task + 1 temporary）
- **前端**：
  - 新增 `AgentMemoryPage.tsx` 三栏布局（AgentSidebar / MemoryBoard / MemoryDetail）
  - 新增 15+ API 函数
  - 新增 CSS：~250 行（`.am-*` 前缀）
  - 路由：`/memory` → `AgentMemoryPage`，旧页面移至 `/memory-shelf`
- **修复的 bug**：
  - seed.py `UnboundLocalError`：P10 section 在 `async with` 外面 + `from app.models.project import Project` 遮蔽顶部导入 → 移入 async with 块 + 删除重复导入
  - `NoReferencedTableError`：FK 引用 `generation_tasks` → 改为 `agent_tasks`
  - Pydantic `tags` 字段缺失：ORM 用 `tags_json`，schema 期望 `tags` → 在 ORM 加 property
  - Router 双重 commit：`session_scope()` 自动 commit + 路由手动 `db.commit()` → 去掉路由手动 commit
- **API 验证**：stats/list/create/detail/promote 全部通过
- TypeScript 编译零错误

## 模型配置页面 Failed to fetch 修复
- **根因**：后端在 8100 端口运行，但前端 `.env.development` 配置 `VITE_API_BASE=http://127.0.0.1:8000`
- **影响范围**：所有页面（ModelsPage 等），API 请求打到错误端口
- **修复**：kill 8100 旧进程，重启 uvicorn 在 8000 端口
- **验证**：`/api/models/providers`、`/api/agent-roles/matrix` 正常，CORS 头正确

## 稳定性改进 (P1-P6 全部完成)
- **P1 进程守护**：uvicorn `--reload` 模式，代码变更自动重启
- **P2 端口统一**：config.py/Dockerfile/.env.development/dev.bat 全部统一为 8000
- **P3 前端健康检查**：`useBackendHealth` hook (15s 轮询 `/health`) + `BackendHealthBanner` 红色提示条
- **P4 一键启停**：`dev.bat` (启动后端+前端) + `stop.bat` (一键停止)
- **P5 日志持久化**：后端日志重定向到 `data/logs/backend.log`
- **P6 Docker**：docker-compose.yml 已有 `restart:unless-stopped` + healthcheck
- **提交**：cd08318，已推送 GitHub

## P0-MODEL-FAILOVER 模型自动选择 + 24h 故障切换 (完成)
- **P0 数据库**: AgentModelBinding 扩 13 字段 (selection_mode/auto_strategy/candidates/fallback/health),
  ModelProvider 扩 15 字段 (circuit breaker/runtime stats/daily metrics), 新增 ModelRuntimeStat/ModelCallEvent 表
- **P1 服务**: ModelCapability(画像) + ErrorClassifier(8 类错误) + ModelSelector(5 策略评分) + 
  CircuitBreaker(closed/open/half_open) + CallRecorder(实时记录)
- **P2 路由**: LLMRouter.resolve() 接入 ModelSelector, chat() 自动 fallback 切换
- **P3 监控**: ProviderHealth 5min 轻量/完整检查, Worker 60s half_open 恢复
- **P4 API**: /preview-selection, /auto-configure, /circuit/reset, /health/full
- **P5 前端**: ProviderAccordion 头部加 health/circuit/sr 徽章, 展开后监控行+重置熔断按钮
  AgentRoleRow 加 selection_mode 徽章
- **迁移**: init_db 的 _COLUMN_BACKFILLS 加 22 列自动 backfill, 无 Alembic 依赖
- **关键修复**: ModelProviderRead schema 需手动加 P0 字段 (Pydantic 不会自动补齐)
- **提交**: 40f4f87, 已推送 GitHub
- **验收**: 17 Agent 一键 auto-configure, 全部 P0-P4 API 端点工作

## P0 Model Failover + NF2 自动工厂 — 整合计划制定
- 完整阅读 `P0_Model_Failover_返工技术方案.md` + `NF2_unified_auto_factory_refactor_plan.md`
- 全仓库代码审查确认 6 个真实 Bug + 7 个功能缺口:
  - 🔴 BUG-1: `_prepare_payload()` 无条件注入 JSON 提示 (L652-679)
  - 🔴 BUG-2: fallback 候选固定 score=0.1 (L431)
  - 🟡 BUG-3: router fallback 只试一次
  - 🟡 BUG-4: fallback 不排除已失败 provider/model
  - 🟡 BUG-5: provider_health 和路由探针两套逻辑
  - 🔴 BUG-6: Worker 不检查 not_before_at，无延迟重试
  - GAP-7~13: AgentRunRecorder, observability API, 前端面板, PromptAutoBinder, 读者页面, 评论自动流, 矩阵硬编码
- 已存在基础设施: PageTopbar(✅), GenrePromptMapping扩展字段(✅), Provider全字段(✅), WorkerHealthLoop(✅)
- 创建 `docs/MASTER_EXECUTION_PLAN.md`: 6 Sprint, 17 commit, ~50-76文件

## S5-S6 完成 + 最终验收

### S5 — 评论自动流 + 审计日志（完成）
- **S5-T1**: `reviews.py` 新增 `POST /reviews/auto-create` 端点，Agent 任务完成后自动写入评论区
  - 支持 `agent_key` → `AgentRole` 自动查找，`author_type` 自动判定 (reader_* → reader_agent, 其他 → chief_agent)
  - 自动入队 comment_triage
  - Schema: `AgentAutoCreateRequest` / `AgentAutoCreateResponse`
- **S5-T2**: 审计日志全栈实现
  - ORM: `models/audit_log.py` (`AuditLog` 表)
  - Schema: `schemas/audit.py`
  - Service: `services/audit_service.py` (`audit_log()` + 便捷函数 `log_model_switch` / `log_prompt_binding_change` / `log_review_action` / `log_agent_task` / `log_settings_change`)
  - Router: `routers/audit.py` (`GET /api/audit/logs` 过滤分页 + `GET /api/audit/logs/recent` + `GET /api/audit/stats/by-event`)
  - 注册到 `main.py`

### S6 — 前端可观测性面板 + 最终验证（完成）
- **S6-T9**: `frontend/src/components/models/ModelObservabilityPanel.tsx`
  - 4 个概览卡片（总调用/成功率/平均延迟/总 Token）
  - Provider 统计表格
  - 最近事件列表
- `frontend/src/api/index.ts`: 新增 4 个 observability API 函数
- `frontend/src/App.tsx`: 注册 `/model-observability` 路由
- TypeScript 编译: 仅 1 个预-existing `PageTopbar.tsx` 错误，所有新建/修改文件零错误

### 最终验证结果
- **Backend import 检查**: 15/15 模块全部导入成功
- **Frontend TypeScript**: 零错误 (除预-existing PageTopbar)
- **修复**: `prompt_auto_binder.py` 导入错误 `app.models.prompts` → `app.models.prompt`
- **交付**: `docs/ACCEPTANCE_CHECKLIST.md` — 含逐项验证命令 + curl 测试用例

## 前端导航栏更新（回应"没有变化"反馈）
- **RailNav.tsx**: 新增两个导航入口
  - "可观测性" → `/model-observability`（模型调用统计面板）
  - "审计日志" → `/audit-logs`（操作审计记录页面）
- **App.tsx**: 注册 `/audit-logs` 路由
- **新增页面**: `pages/AuditLogPage.tsx`（审计日志列表页）
- **新增 API**: `listAuditLogs()` in `api/index.ts`
- TypeScript 编译: 零错误（仅预-existing PageTopbar）

## P0+NF2 未完成任务全量补齐（本轮完成）

### 后端补齐
- **修复**: `worker.py:595` `task_row.instruction` bug → 改为从 `task_row.payload.get("rewrite_instruction")` 取
- **新建**: `models/prompt_auto_fill.py` — 3 个新模型 (PromptAutoFillBatch, PromptRecommendationLog, PromptTemplatePerformance)
- **新建**: `routers/prompt_matrix.py` — 9 个端点 (preview/apply/rollback/recommendations/lock/unlock/coverage/performance)
- **扩展**: `routers/reviews.py` — 5 个读者API端点 + auto-flow状态API
- **扩展**: `database.py _COLUMN_BACKFILLS` — agent_tasks 4 个新列 (retry_count, max_retries, last_failure_type, last_fallback_summary)
- **注册**: `main.py` 注册 prompt_matrix 路由

### 前端模型配置UI (10个新组件)
- `BindingModeSwitch.tsx` — 自动/手动/手动+备用三模式切换
- `AutoStrategySelect.tsx` — 5种自动策略选择
- `CandidateProviderPicker.tsx` — Provider多选
- `CandidateModelPoolEditor.tsx` — 候选模型池编辑
- `FallbackCandidateEditor.tsx` — Fallback候选编辑
- `ModelSelectionPreviewPanel.tsx` — 选模预览面板
- `ModelFailoverTimeline.tsx` — Failover时间线
- `ProviderHealthFullModal.tsx` — 健康检查弹窗
- `AutoConfigureToolbar.tsx` — 一键自动配置
- `CircuitBreakerBadge.tsx` — 熔断器状态徽标
- **改造**: `AgentRunDetailPanel.tsx` — 集成上述组件，根据selection_mode动态渲染

### 前端Prompt矩阵组件 (6个新组件)
- `PromptMatrixTable.tsx` — 后端驱动的Agent×Genre矩阵
- `PromptCell.tsx` — 单元格(模板名+匹配%+状态标签)
- `PromptAutoFillPanel.tsx` — 自动填充操作面板
- `PromptRecommendationDrawer.tsx` — 推荐详情抽屉
- `PromptCoverageBar.tsx` — 覆盖率进度条
- `PromptBatchHistory.tsx` — 批次历史

### 前端新页面
- `ReaderAgentsPage.tsx` — 5读者Agent编辑中心
- `ReaderAgentDetailPage.tsx` — 单个读者详情编辑
- `AutomationAuditPage.tsx` — 自动化审计看板

### 前端评论评审组件 (5个新组件)
- `ReviewAutoFlowPanel.tsx` — 流水线状态
- `ReviewCommentFeed.tsx` — 评论流
- `ReviewGroupPanel.tsx` — 问题分组面板
- `ReviewDecisionTimeline.tsx` — 裁决时间线
- `ReviewDebugMenu.tsx` — 调试菜单

### 前端改造
- `GenrePromptMatrixPage.tsx` — 移除硬编码AGENT_ROWS/GENRE_LIST，改为后端驱动+Section分组
- `RailNav.tsx` — 新增"读者📖"+"审计🔍"导航入口
- `App.tsx` — 新增3条路由
- `api/index.ts` — 新增16个API函数
- `PageTopbar.tsx` — 修复title字段缺失的TS错误

### 后端测试 (8个文件)
- test_llm_client_prompt_mode.py, test_model_selector_failover.py, test_circuit_breaker.py
- test_llm_router_fallback.py, test_provider_health_service.py, test_worker_retry_delay.py
- test_agent_run_recorder.py, test_prompt_auto_binder.py

### 验证
- **TypeScript**: npx tsc --noEmit 零错误
- **Vite build**: 成功 (572KB JS + 143KB CSS)
- **Python语法**: 所有新建/修改文件通过 ast.parse

## P0 模型可观测性重构 + 工作台仪表盘化 + 冗余标题清理 (ea78597)

### P0-A 冗余标题清理
- `ShelfLayout.tsx`: title改可选prop, 标题区条件渲染 `{(title||subtitle) && ...}`
- `ProjectsPage.tsx`: 删除 title/subtitle/breadcrumb 冗余props
- `Dashboard.tsx`: 删除 `<header className="page-header">` 大标题块
- `ModelObservabilityPanel.tsx`: 删除冗余 `<h2>` 标题

### P0-B 可观测性数据补全(后端)
- `model_call_event.py`: 新增15个字段(event_type/event_category/level/chapter_id/step_key/provider_name/fallback_from_provider/model/fallback_to_provider/model/summary/detail_json/cache_hit/request_id/error_code)
- `database.py _COLUMN_BACKFILLS`: 追加15条 ALTER TABLE
- `model_call_recorder.py`: record_selection/success/failure/fallback_success 写入新字段+自动生成summary
- `router.py chat()`: 新增 chapter_id/step_key/project_id 参数
- `model_observability.py` 路由: 新增4个端点(/models /agents /slow-requests /failures)
- `/events` 端点: 新增6个过滤参数(event_type/event_category/level/model_name/project_id/chapter_id)
- `/summary` 端点: 新增 fallback_count/failed_count/timeout_count/cache_hit_rate/p95/input_tokens/output_tokens
- `agent_run_recorder.py`: get_recent_events新增6个过滤参数+输出新字段; get_summary新增7个统计字段

### P0-C 可观测性UI重构(前端)
- 新建 `model-observability/` 目录: 10个组件
  - ObservabilityFilterBar: 5按钮时间范围+3下拉+6状态pill
  - ObservabilityKpiGrid: 2×4 KPI网格
  - ProviderHealthTable: 12列+可展开模型列表
  - ModelHealthTable: 8列+成功率颜色+agent pill
  - AgentCallMatrix: 5 Section分组+卡片式
  - FailureReasonChart: 水平条形图(failure_type)
  - ObservabilityEventStream: 卡片式+左边框颜色+无限滚动
  - EventDetailDrawer: 360px右侧抽屉
  - SlowRequestList: 延迟颜色3级
  - FallbackTimeline: 垂直时间线
- 重写 ModelObservabilityPanel: 移除Tailwind改用CSS变量, 集成10个新组件, 15s自动刷新
- api/index.ts: 新增4个API函数(getObservabilityModels/Agents/Failures/SlowRequests)

### P0-D Agent模型绑定弹窗
- `AgentRoleEditorModal.tsx`: 3-Tab(基础信息/模型绑定/Prompt绑定)
- `AgentRunDetailPanel.tsx`: 新增"高级配置"按钮

### P1 工作台仪表盘化
- `DashboardKpiCards.tsx`: 4 KPI(今日产出/质量分/API健康/成本)
- `AgentPipelineVisualization.tsx`: 13节点流水线+状态颜色
- `MemoryLayerCard.tsx`: 4层记忆卡片
- `Dashboard.tsx`: 集成3个新组件

### 验证
- **TypeScript**: npx tsc --noEmit 零错误
- **Vite build**: 成功 (613KB JS + 143KB CSS)
- **Python语法**: 所有文件 ast.parse 通过

## P0 Agent 编辑弹窗补齐 API Provider / Model 绑定 (commit 待推送)

### 根因
- `/models` 页面编辑 Agent 时使用了旧 `AgentRoleEditor`（只有基础信息），而项目里已有三 Tab 的 `AgentRoleEditorModal`（基础信息/模型绑定/Prompt 绑定），只是没有接上

### P0-A: ModelsPage 改用三 Tab 编辑器
- `ModelsPage.tsx`: 
  - 新增 `AgentRoleEditorModal` 导入
  - 拆分为两个弹窗: 新增→旧 `AgentRoleEditor`(create模式), 编辑→新 `AgentRoleEditorModal`(三Tab)
  - 新增 state: `createEditorOpen`, `bindingEditorOpen`, `editingRoleId`
  - `onEditAgent` 从 matrix.items 找到完整 `AgentRoleMatrixItem`(含 role/binding/prompt_binding), 传给新编辑器
  - 保存后自动刷新矩阵 + 成功提示

### P0-B: index.ts 导出补全
- `components/models/index.ts`: 新增 `AgentRoleEditorModal` 和 `ProviderModelPicker` 导出

### P0-C/D/E/F: 编辑器增强
- **新增** `ProviderModelPicker.tsx`: Provider下拉(仅enabled) + 模型下拉/手动输入 + 拉取模型列表 + 健康检查按钮
- **重写** `FallbackCandidateEditor.tsx`: Provider下拉替代数字输入 + 权重列 + 重复检测
- **增强** `AgentRoleEditorModal.tsx`:
  - 集成 `ProviderModelPicker` (manual/manual_with_fallback模式)
  - `FallbackCandidateEditor` 传入 providers 列表实现 Provider 下拉
  - 新增 `validate()` 校验函数: manual模式必须选Provider+Model, auto模式至少1个候选, manual_with_fallback至少1个fallback或启用自动fallback
  - stub/mock-fast 警告: 自动检测 stub Provider 或 mock-* 模型, 显示黄色警告条

### P0-G/H: 后端验证
- **后端已正确实现**: model_selector.py 深度读取 AgentModelBinding(3种模式), LLMRouter 和 AgentRoleRunner 都正确传递 Provider/Model
- **Fallback 安全**: 系统明确排除 mock:// Provider, 不会在生产静默使用 stub
- **stub 守卫**: UI 编辑器内检测并显示警告, fallback 逻辑排除 mock://

### 验证
- **TypeScript**: npx tsc --noEmit 零错误
- **Vite build**: 成功 (615KB JS + 145KB CSS)
- **Python语法**: 12+4 新建文件全部通过 ast.parse

## P0 任务中心三层架构 + 项目/拆书边界 + P2 Bug修复 (commit 待推送)

### P0-1: 任务中心三层架构 (后端)
- `models/task.py` AgentTask: 新增 11 个字段 (parent_task_id/visibility/domain/task_kind/material_id/run_id/stage_key/progress_current/progress_total/display_title/summary_json)
- `core/database.py` _COLUMN_BACKFILLS: 追加 11 条 ALTER TABLE agent_tasks
- `routers/tasks.py` GET /api/tasks: 新增 visibility/domain/task_kind/parent_task_id 参数，默认过滤 visibility=user
- `services/deepstudy/coordinator.py`: execute_run 创建 user-visible 父任务 (domain=deepstudy)，每阶段更新进度和成本
- `scripts/migrate_task_visibility.py`: 历史 study_character/study_event → visibility=internal + 为已完成 DeepStudyRun 聚合父任务

### P0-2: 任务中心 UI 重构 (前端)
- `TasksPage.tsx`: 完全重写 (246→320行)
  - 删除表格日志布局 (study_character 平铺问题解决)
  - 新增顶部统计条 (运行中/等待/失败/已完成/成本/Token)
  - 新增分类 Tab: 全部/写作/拆书/模型/讨论/记忆/导出/失败
  - 新增任务卡片: 进度条+阶段轨道+产物摘要+domain标签
  - 展开详情: user步骤表格 + 内部事件 (默认折叠)
  - 重试/取消功能保留
- `TasksPage.css`: 新增 ~120 行 (.tasks-stats-bar/.task-card-* 系列)
- `types/index.ts` AgentTask: 新增 11 个可选字段
- `api/index.ts` listTasks: 新增 4 个参数 (visibility/domain/task_kind/parent_task_id)

### P0-3: 项目/拆书业务边界
- `models/project_study_link.py`: 新建 ProjectStudyMaterialLink 表 (project_id + material_id UNIQUE)
- `models/__init__.py`: 注册新模型
- `database.py`: __new_table__ 标记自动建表

### P0-4: Worker 状态拆分
- `routers/worker.py`: 新增 GET /worker/multi-status 端点 (Writing/DeepStudy/Model 各自独立状态)

### P2: Bug修复 (7项)
- `services/detail_guard.py`: CRITICAL_SECRET_TOKENS 配置 + check_critical_secrets()
- `services/foreshadow_guard.py`: 新建，50章沉默阈值检测
- `context_compiler.py`: LIMIT 50→200 + category+importance 加权排序
- `models/task.py` AgentStep: 新增 is_mock 列 + database.py 迁移
- `agents/base.py`: 当 model_name 以 "mock-" 开头时标记 is_mock=True
- `models/study.py` GraphEdge: 新增 count 列 + UNIQUE(source,target,relation)
- `routers/graph.py` + `routers/study.py`: graph edge upsert (ON CONFLICT DO UPDATE weight=weight+..., count=count+1)

### 验证
- **TypeScript**: npx tsc --noEmit 零错误
- **Vite build**: 成功 (615KB JS + 145KB CSS)
- **Python语法**: 所有新建文件通过 ast.parse
- 文件变更: ~20个文件

## P1 + P2 + P3 剩余任务全量补齐 (commit 待推送)

### D2: OpenAPI → TypeScript 自动生成
- `openapi-codegen.config.ts`: 配置 @hey-api/openapi-ts
- `package.json`: codegen/codegen:file 脚本已就绪
- `backend/openapi.json`: 190 路径的 OpenAPI 3.1.0 schema
- `frontend/src/api/generated/`: 生成目录已建立
- 依赖已安装 (@hey-api/openapi-ts, @hey-api/client-fetch)

### P1-B: Reader×5 + Discussion + Skill 闭环 UI
- **新建** `ReaderFeedbackPanel.tsx`: 5 Readers 评分+情绪+评语卡片, 30s 刷新
- **新建** `DiscussionLoopCard.tsx`: 讨论室线程+7天回收倒计时+Skill关联
- **新建** `SkillGeneratedCard.tsx`: 沉淀技能展示(plot/character/pacing分类)
- `components/dashboard/index.ts`: 导出 3 个新组件
- `Dashboard.tsx`: 集成到仪表盘布局(2列+全宽行)

### B1: 两套行为模式系统统一
- `models/behavior_card.py`: 新增 source_pattern_id 列
- `database.py`: behavior_cards.source_pattern_id 迁移
- `routers/behavior.py`: GET /behavior/patterns 从 behavior_cards 主查, source=all|cards|legacy
- `scripts/migrate_behaviors_to_cards.py`: 迁移脚本, 旧数据→新表
- `StudyPage.tsx` PatternLibrary: B1统一系统横幅+数据源选择器
- `api/index.ts` listBehaviorPatterns: 新增 source 参数

### B3: Worker 水平扩展 (多域分区)
- `workers/worker.py`:
  - 新增 WorkerDomain Literal 类型 + DomainWorkerStatus 类
  - 新增 domain_statuses 字典 (6域状态追踪)
  - 新增 domain_concurrency 字典 (writing=2/deepstudy=1/discussion=3/memory=1/model=2)
  - _tick() 按域并发限制选取任务
  - 4 个后台循环更新域状态 + 同步全局注册表 worker_domain_status
- `routers/worker.py`: /multi-status 从全局注册表读取, 新增 discussion/memory worker 字段
- `types/index.ts`: 新增 MultiWorkerStatus 类型
- `api/index.ts`: 新增 multiWorkerStatus() API
- `Dashboard.tsx`: 新增 DomainWorkersCompact 组件 (写作●研读○讨论●记忆○模型●)

### 验证
- **TypeScript**: npx tsc --noEmit 零错误
- **Vite build**: 成功
- **Python语法**: worker.py/router.py/behavior.py/behavior_card.py 全部通过

## P0 DeepStudy 自动联动重构 — 拆书→图谱/行为模式/技巧 (commit 待推送)

### 核心设计理念
- 拆书不再是手动按钮工具箱，而是 DeepStudy 自动知识生产流水线
- 用户上传 → 启动 DeepStudy → 自动生成图谱/行为模式/技巧 → 查看结果
- 所有内部工序(批量抽人物/抽事件/提取行为模式/分析人物关系/手动导入图谱)均为后端 Agent 自动完成
- Debug 模式 (VITE_ENABLE_DEEPSTUDY_DEBUG=true) 保留内部工序入口

### P0-1: StudyPage 移除手动按钮
- `StudyPage.tsx`: 大规模重构 (1314→~1150行)
  - 删除手动按钮: 批量抽人物/批量抽事件/试抽5章/提取行为模式/分析人物关系
  - 删除 RelationshipsModal 组件 (人物关系模态+应用图谱)
  - 删除 BulkProgressBar (旧批量进度条)
  - 删除相关 handler: onRunStudyBulk/onExtractBehaviors/onAnalyzeRelationships/onApplyRelationships
  - 删除相关 state: bulkProgress/bulkTimers/relMaterialId/relData/relLoading/relApplyMsg/selectedSuggestions/relationInputs
  - 删除未使用 imports: runStudyBulk/getTask/extractStudyBehaviors/getStudyRelationships/applyStudyRelationships/getStudyForeshadows
  - **新增**: "🚀 启动 DeepStudy" 一键按钮 → 调用 startDeepStudyRun + 自动轮询 getDeepStudyRun
  - **新增**: DeepStudyProgressCard 组件 — 14阶段进度条 + 当前阶段 + 成本/token/产物统计
  - **新增**: 自动产物预览行 (行为模式/伏笔/图谱统计，来自DeepStudy自动联动)
  - 新增 "🌐 查看图谱" 按钮 (DeepStudy完成后可见)
  - 新增 deepstudyRuns/deepstudyLaunchBusy state
  - 新增 pollDeepStudyRun 轮询函数

### P0-2: DeepStudyCoordinator 真正跑 DAG + Worker
- **新建** `services/deepstudy/` 目录 (11个文件):
  - `__init__.py`, `job_graph.py` (14阶段DAG+依赖), `event_bus.py` (异步事件总线)
  - `coordinator.py` (DeepStudyCoordinatorAgent — 主执行循环), `stage_result_store.py`
  - `graph_materializer.py` (自动增量建图+GraphFinalize), `behavior_miner.py` (自动行为模式)
  - `technique_miner.py` (自动写作技巧), `writing_context_sync.py`, `knowledge_indexer.py`, `auto_repair.py`
- **新建** `workers/deepstudy_worker.py` — deepstudy_tick() 轮询 queued/running runs
- **修改** `workers/worker.py` — 注册 deepstudy worker 后台任务 (10s 间隔)
- 所有时间戳使用 `datetime.now(timezone.utc)` (非已弃用的 utcnow())
- 阶段完成自动发布事件 → graph_materializer/behavior_miner/technique_miner 自动联动

### P0-3/4/5: 综合
- `graph_materializer.py`: 多阶段增量节点/边生成 (book/chapter/entity/event/foreshadow/behavior/technique)
- `behavior_miner.py`: 按章节批次挖掘 → 全书整合 → 自动入库
- `technique_miner.py`: 从行为模式+场景节拍+伏笔链提炼写作技巧
- `knowledge_indexer.py`: 全文检索索引
- `auto_repair.py`: 失败阶段+下游传递闭包自动修复

### P0-6: StudyLibraryPage 简化
- `StudyLibraryPage.tsx`: 普通UI只显示 full + repair_failed 两种启动模式
- entities_only/relationships_only/behaviors_only/techniques_only 移至 Debug 模式

### 验证
- **TypeScript**: npx tsc --noEmit 零错误
- **Vite build**: 成功 (612KB JS + 143KB CSS)  
- **Python语法**: 12个新建文件全部通过 ast.parse
- 文件变更: 14个新建 + 3个修改 (StudyPage.tsx, StudyLibraryPage.tsx, worker.py)
