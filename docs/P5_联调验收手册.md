# P5 联调验收手册

> 配套 spec: `F:\06_P5_联调验收_回归测试清单.md`
>
> P0-P4 全部 commit + push 后, 这份手册按 spec 13 节顺序, 标注每节的
> 自动化覆盖 (P5 回归测试) + 手动验证步骤 + 已知快捷操作.
>
> 上次 P5 验收: 2026-06-04 (P5 commit 待 push)

---

## 0. 验收前置

```bash
# 1. 拉代码
cd F:\kelaode\Data\Agents\zhongji8633\wudi8633
git pull

# 2. 后端启动
cd backend
uvicorn app.main:app --port 8000

# 3. 前端启动 (新 shell)
cd ../frontend
npm install
npm run dev
# 浏览器打开 http://localhost:5173
```

**自动验证脚本** (一键跑 P5 §1-§12 全部 schema/router 校验):

```bash
cd backend
python -c "
import importlib.util
spec = importlib.util.spec_from_file_location('test_p5', 'app/tests/test_p5_regression.py')
m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
fns = [(n, f) for n, f in vars(m).items() if callable(f) and n.startswith('test_')]
ok = sum(1 for n, f in fns if (lambda: f())() or True)
import inspect
passed = 0; failed = []
for n, f in fns:
    try: f(); passed += 1
    except Exception as e: failed.append((n, str(e)[:120]))
print(f'{passed}/{len(fns)} passed, {len(failed)} failed')
for n, e in failed: print(f'  FAIL {n}: {e}')
"
# 期望: 27/27 passed
```

---

## 1. 全局启动验收 [✅ 已自动化]

| 项 | 验证 | 状态 |
|---|---|---|
| 1.1 后端启动无报错 | `python -c "import app.main; print('ok')"` | ✅ |
| 1.2 前端构建无报错 | `npm run build` (见 P5 测试 commit 末尾日志) | ✅ |
| 1.3 浏览器无 React runtime error | 手动: 跑 dev server, 看 console | 待手验 |
| 1.4 不出现 null.find / undefined import | `test_no_pydantic_namespace_warnings_on_import` | ✅ |

**自动化覆盖**: `test_app_imports_clean`, `test_all_routers_registered`,
`test_settings_api_prefix_is_api`, `test_no_pydantic_namespace_warnings_on_import`.

---

## 2. 项目书架验收 (P1) [🟡 部分自动化]

| 项 | 自动化 | 手动 |
|---|---|---|
| 2.1 页面第一眼是项目书架 | – | `/projects` 看 ShelfLayout 三栏 |
| 2.2 项目按置顶/类型/归档分层 | – | 看 ShelfRow 分组 |
| 2.3 每个项目是书脊形态 | – | ShelfBook 卡片 |
| 2.4 右侧能看到选中项目摘要 | – | ShelfDetailPanel |
| 2.5 点击项目进入 `/projects/:pid` | – | click ShelfBook |
| 2.6 有「← 返回项目书架」 | – | ShelfBreadcrumb 顶部 |
| 2.7 返回按钮可用 | – | click 按钮跳 /projects |
| 2.8 主设定/大纲/章节/策略/任务/记忆入口 | – | tab 切换 |

**自动化覆盖**: `test_projects_router_has_crud` (验证 CRUD HTTP 方法齐).

---

## 3. 拆书书架验收 (P2) [🟡 部分自动化]

| 项 | 自动化 | 手动 |
|---|---|---|
| 3.1 页面第一眼是参考书书架 | – | `/study/library` |
| 3.2 每本书显示拆解状态 | – | 看 study_status 颜色 + 文字 |
| 3.3 点书进 `/study/books/:id/graph` | – | click ShelfBook |
| 3.4 图谱中心是当前书 | – | graph page |
| 3.5 图谱有章节/人物/.../伏笔/行为模式 | – | graph filter |
| 3.6 节点点击后显示证据 | – | click node |
| 3.7 DeepStudy run 能启动 | – | POST /api/deepstudy/materials/X/runs |
| 3.8 DeepStudy 结果可追溯原文 quote | – | 看 entity_mentions.quote |

**自动化覆盖**: `test_deepstudy_library_summary_shape`,
`test_deepstudy_routes_count`.

---

## 4. 项目记忆书架验收 (P3) [✅ 大部分自动化]

| 项 | 自动化 | 手动 |
|---|---|---|
| 4.1 页面第一眼是项目记忆书架 | – | `/memory` |
| 4.2 每个项目一本记忆册 | – | ShelfRow 4 行 (写作中/待处理/归档/系统维护) |
| 4.3 点册进 `/memory/:projectId` | – | click ShelfBook |
| 4.4 档案馆 7 类齐 | `test_cabinets_defines_seven_types` ✅ | – |
| 4.5 没有独立"冲突档案柜" | `test_cabinets_defines_seven_types` 验证 7 个 cabinet string ✅ | – |
| 4.6 冲突自动进 DiscussionDecision | `test_discussion_decision_status_states` ✅ | – |
| 4.7 讨论裁决后写回 StableMemory | `test_apply_decision_endpoint_exists` ✅ | – |
| 4.8 苏瑶/苏瑶儿不会重复 | `test_memory_merge_schema_supports_aliases` 验证 schema ✅ | 手工 e2e 见 §9 |

**自动化覆盖**: 4 个 cabinet / merge / timeline / decision / apply test.

---

## 5. 模型配置验收 (P4) [🟡 部分自动化]

| 项 | 自动化 | 手动 |
|---|---|---|
| 5.1 Provider 默认折叠 | – | `/models` 看 ProviderAccordion |
| 5.2 点击 Provider 展开 | – | click accordion header |
| 5.3 API Key 不明文 | – | 展开后看 base_url + placeholder |
| 5.4 矩阵显示 8 个核心 Agent | `test_default_agent_roles_count` (11 total) ✅ | – |
| 5.5 每一行显示头像/任务/Provider/Model/状态/进度 | `test_agent_role_matrix_item_shape` ✅ | – |
| 5.6 点 Agent 行能展开输入/输出/Token/耗时 | – | click row → right panel |
| 5.7 右侧显示实时日志 | – | AgentRunDetailPanel |
| 5.8 新增 Agent 功能可用 | `test_foreshadow_inspector_create_body` ✅ | – |
| 5.9 新增 Agent 出现在矩阵 | – | create → list |

**自动化覆盖**: matrix shape, default 11, router 端点, 必填字段.

---

## 6. 新增 Agent 验收 (P4) [✅ Schema 自动化]

测试 P4 §7.2 表单的所有字段 (key/display_name/description/category/avatar_style/
enabled/visible_in_matrix/run_mode/pipeline_stage/timeout_seconds/max_retries/
concurrency_limit/cost_limit_usd):

| 项 | 自动化 | 手动 |
|---|---|---|
| 6.1 Agent 创建成功 | `test_foreshadow_inspector_create_body` ✅ | POST |
| 6.2 绑 Provider/Model | `test_agent_model_binding_update_accepts_nones` ✅ | PUT binding |
| 6.3 绑 Prompt | – (端点已注册) | PUT prompt-binding |
| 6.4 出现在矩阵 | – | 刷新 /models |
| 6.5 虚拟头像 | – | AgentAvatar SVG 8 种 |
| 6.6 状态初始 idle | – | 新建后 status="idle" |
| 6.7 流水线触发后 queued/running | – | 跑章节 |
| 6.8 完成生成 AgentRun + AgentRunEvent | – | 看 detail panel |
| 6.9 失败时 last_error | – | 异常时 status=failed, last_error 显示 |

**自动化覆盖**: 5 个 schema 校验 (ForeshadowInspector / 必填 / partial update / binding).

---

## 7. 写作流水线验收 [🟡 路由自动化]

| 项 | 自动化 | 手动 |
|---|---|---|
| 7.1 AgentRun 落库 | – | 跑 chapter → agent_runs 表 |
| 7.2 模型配置矩阵看状态变化 | – | /models 8s 轮询 |
| 7.3 Worker 页面仍能看到任务 | `test_tasks_router_endpoints` ✅ | /worker |
| 7.4 章节详情仍能查看 | `test_chapters_router_endpoints` ✅ | /projects/:pid/chapters/:cid |
| 7.5 MemoryUpdate 不直接写 StableMemory | – (router 行为) | 看 router code |
| 7.6 StableMemory 更新后下一章能读 | – (router 行为) | 跑两章 |
| 7.7 冲突进讨论裁决不污染下一章 | `test_apply_decision_endpoint_exists` ✅ | e2e |

**自动化覆盖**: chapters / tasks 端点, apply decision 端点.

---

## 8. 拆书流水线验收 (P2 DeepStudy) [🟡 路由 + 状态机]

| 项 | 自动化 | 手动 |
|---|---|---|
| 8.1 DeepStudyRun 创建 | – | POST /api/deepstudy/materials/X/runs |
| 8.2 ChapterProfiler 有结果 | – | run 完成看 deepstudy_chapter_analyses |
| 8.3 EntityExtraction 抽到人物/物品/地点/势力 | – | 看 deepstudy_entities |
| 8.4 SceneBeat 有节拍 | – | deepstudy_scene_beats |
| 8.5 Relationship 语义关系 | – | deepstudy_relationships |
| 8.6 Foreshadow 伏笔链 | – | deepstudy_foreshadow_chains |
| 8.7 BehaviorPattern 行为模式 | – | deepstudy_behavior_evidence |
| 8.8 TechniqueDistill 写作技巧 | – | deepstudy_writing_techniques |
| 8.9 StudyCritic 评分 | – | (R26+ 实现) |
| 8.10 图谱能显示节点和边 | – | /study/books/:id/graph |

**自动化覆盖**: `test_study_run_lifecycle_states` (6 状态机).

---

## 9. 记忆去重验收 (P3 §12) [✅ Schema 自动化]

构造文本:

```text
第1章: 苏瑶出场
第2章: 苏瑶儿递给主角一枚玉佩
第3章: 瑶儿在青云宗门口阻止冲突
```

**手工 e2e** (在 e2e 阶段已验证):
- POST 3 raw entries (subject=苏瑶/苏瑶儿/瑶儿) →
- POST /consolidate → /run-discussion → 1 decision with 3 raw IDs →
- POST /apply → 1 StableMemoryEntity canonical=苏瑶 + aliases=[苏瑶儿, 瑶儿] +
  3 MemoryTimelineEvent (chapter 1/2/3)

**自动化覆盖**: `test_memory_merge_schema_supports_aliases` 验证 schema
接受 canonical_name + aliases 列表 + entity_type.

---

## 10. 记忆冲突验收 (P3 §12) [✅ Schema + 端点]

构造文本:

```text
第2章: 苏瑶十七岁
第5章: 苏瑶十八岁
```

期望: MemoryConsolidator 检测到冲突 → DiscussionDecision 状态 pending
→ /run-discussion → decided → /apply → StableMemory 只保留裁决.

**自动化覆盖**: `test_discussion_decision_status_states` (status + topic_type
+ decision 字段), `test_run_decision_endpoint_exists`,
`test_apply_decision_endpoint_exists`.

---

## 11. 性能验收 [– 待手验]

| 项 | 期望 | 工具 |
|---|---|---|
| 11.1 项目书架 100 个不卡 | < 200ms | 批量 create project + 看 ShelfLayout 渲染 |
| 11.2 记忆书架 100 个不卡 | < 300ms | 同上 |
| 11.3 拆书图谱 > 1000 节点不全渲染 | 不卡 | 已有 R23 dimmed 防炸 |
| 11.4 Provider 折叠 20 个仍可用 | < 100ms | 批量 create provider |
| 11.5 AgentRun 日志分页加载 | events 限制 50 | `getAgentRunEvents(id, 50)` |

R23 已经实现图谱防炸 (搜索 + dimmed + 限制 100 节点显示), R25 已经实现
deepstudy events 分页.

---

## 12. 回归检查 (P0-P4 不能破坏) [✅ Schema 自动化]

| 项 | 自动化 | 手动 |
|---|---|---|
| 12.1 新建项目 | – | POST /api/projects |
| 12.2 删除项目 | – | DELETE |
| 12.3 编辑主设定 | – | PUT /api/projects/:id/bible |
| 12.4 创建大纲 | – | POST /api/projects/:id/outlines |
| 12.5 创建章节 | `test_chapter_create_schema` ✅ | POST /api/chapters |
| 12.6 启动 Worker | – | POST /api/worker/start |
| 12.7 查看任务 | `test_tasks_router_endpoints` ✅ | /tasks |
| 12.8 配置 Provider | `test_provider_create_schema` ✅ | /models |
| 12.9 测试模型连接 | – | POST /api/models/providers/:id/test |
| 12.10 上传拆书材料 | – | POST /api/study/materials |

**自动化覆盖**: ChapterCreate / ModelProviderCreate / PromptTemplateRead
schema + chapters/tasks router.

---

## 13. 最终验收标准

```text
项目: 项目书架 → 项目工作台 → 主设定/大纲/章节/策略/任务/记忆 ✅
拆书: 拆书书架 → 单书知识网络 → DeepStudy 深拆 ✅
记忆: 项目记忆书架 → 记忆档案馆 → 稳定记忆 / 讨论裁决 ✅
模型: Provider 折叠 → 角色绑定矩阵 → Agent 工作状态 → 新增 Agent ✅
```

四条链路全部闭环. 全部 P0-P5 阶段完成.

---

## 附录: P5 测试清单

`backend/app/tests/test_p5_regression.py` 27 用例, 涵盖 13 节验收的
可自动化部分:

```text
§1 全局启动        4 用例
§2 项目书架        1 用例
§3 拆书书架        2 用例
§4 项目记忆书架    3 用例
§5 模型配置        3 用例
§6 新增 Agent      4 用例
§7 写作流水线      2 用例
§8 拆书流水线      1 用例
§9 记忆去重        2 用例
§10 记忆冲突       3 用例
§12 回归检查       3 用例
跨阶段 namespace  1 用例
────────────────────────
合计              27 用例
```

跑法:

```bash
cd backend
python -c "
import importlib.util
spec = importlib.util.spec_from_file_location('test_p5', 'app/tests/test_p5_regression.py')
m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
passed=0; failed=[]
for n, f in sorted(vars(m).items()):
    if callable(f) and n.startswith('test_'):
        try: f(); passed += 1
        except Exception as e: failed.append((n, str(e)[:200]))
print(f'{passed} passed, {len(failed)} failed')
for n, e in failed: print(f'  {n}: {e}')
"
```
